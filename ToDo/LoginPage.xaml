<Page
    x:Class="ToDo.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#03A9F4">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="0.5*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel  Margin="0,50,0,0" Grid.Column="0" Grid.Row="1"  HorizontalAlignment="Center" Width="200">

            <TextBlock  x:Name="loginTextBox" x:Uid="loginTextBox"   >Please insert your username:</TextBlock>
            <TextBox x:Name="usernameTextBox"/>
        
            
        </StackPanel>

        <StackPanel Margin="0,0,0,0" Grid.Column="0" Grid.Row="2"   >
            <Button Click="onClickLoginButton" x:Name="loginButton" x:Uid="loginButton" Background="#CDDC39" HorizontalAlignment="Center" />
            <Button Click="onClickAboutButton"  x:Name="aboutButton" x:Uid="aboutButton"  Background="#B3F5FC" Margin="0,10,0,0"  HorizontalAlignment="Center"/>
        </StackPanel>


    </Grid>
</Page>
